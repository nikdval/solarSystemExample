const initialState = {
    planets:[],
    loading: false,
    error: null,
  };
  
  const solarSystemReducer = function (state = initialState, action) {
    // switch (action.type) {
    // }
    return state;
  };
  
  export default solarSystemReducer;
  